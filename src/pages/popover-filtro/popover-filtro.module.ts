import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PopoverFiltroPage } from './popover-filtro';

@NgModule({
  declarations: [
    PopoverFiltroPage,
  ],
  imports: [
    IonicPageModule.forChild(PopoverFiltroPage),
  ],
})
export class PopoverFiltroPageModule {}
